import { defineApp } from "convex/server";

// Phase 2: 段階的認証統合 - 基本設定から開始
const app = defineApp();

// Phase 2.2で認証機能を統合予定
// import auth from "./auth.config.js";
// app.use(auth);

export default app;